# Development / test tooling only. Not required at runtime.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
pytest-cov>=5.0.0
pytest-benchmark>=4.0.0
hypothesis>=6.100.0
bandit>=1.7.0
mutmut>=3.2.0

# Presentation and submission media pipeline (scripts/presentation/).
# After installing playwright, also run: python -m playwright install chromium
playwright>=1.55.0
imageio-ffmpeg>=0.6.0
